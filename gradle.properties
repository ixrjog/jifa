# Network timeouts for dependency resolution (ms). Default socket timeout is 30s,
# which caused "Read timed out" when downloading artifacts on slow networks.
systemProp.org.gradle.internal.http.connectionTimeout=60000
systemProp.org.gradle.internal.http.socketTimeout=120000
